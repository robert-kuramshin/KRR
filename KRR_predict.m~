
function [value]=KRR_predict(x,lambda)
N = length(x)
k=zeros(N_test,N_train);
for j=1:N_train
 for i=1:N_test
    k(i,j)=exp(-norm(x_train(j,:)-x_test(i,:)));
 end
end

y_predicted=zeros(N_test,1);

for i=1:N_test
    y_predicted(i,1)= y_train'*((K_gauss+ lambda*eye(N_train))\k(i,:)');
end
end